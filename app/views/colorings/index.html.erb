

<div class="container">
	<div class="marginTopMax"></div>
	<div class="row">
		<% @colorings.each do |coloring| %>
		<div class="col-md-6">
			<%= link_to(image_tag(coloring.coloring.url(:medium), alt: coloring.title, class: "coloring"), coloring_path(coloring)) %>
		</div>
		<% end %>
	</div>
	<div class="marginTopMax"></div>
</div>